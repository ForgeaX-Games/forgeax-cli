{
  "models": {
    "showThinking": false,
    "maxRetries": 3,
    "timeout": -1
  },
  "coalesceMs": 0,
  "maxIterations": 250,
  "defaultStatus": "plan_mode",
  "capabilities": {
    "global": "all",
    "disable": [
      "#task_collaboration", "#heartbeat", "#memory",
      "#skills"
    ],
    "config": {
      "workspace": { "operationLevel": "read-only" }
    }
  },
  "defaultDir": "."
}
