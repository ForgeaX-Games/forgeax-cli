// Reserved for cross-loader shared types. Currently empty after slot watch
// declarations were removed (slots track their own content via mtime caches
// or by reading fresh every turn — see ref_slot_authoring.md).
export {};
