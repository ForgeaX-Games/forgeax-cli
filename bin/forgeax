#!/usr/bin/env bash
# Resolve symlinks to find the real project root (works with pnpm link --global)
SOURCE="$(readlink -f "$0")"
PROJECT_DIR="$(cd "$(dirname "$SOURCE")/.." && pwd)"
exec "$PROJECT_DIR/node_modules/.bin/tsx" "$PROJECT_DIR/src/ctl-command/gateway-ctl/index.ts" "$@"
